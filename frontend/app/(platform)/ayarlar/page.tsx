import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function SettingsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Sistem"
      title="Ayarlar"
      description="Bildirimler, güvenlik tercihleri ve deneyim ayarlarını yöneten kontrol alanı."
      badge="Control room"
    />
  );
}
