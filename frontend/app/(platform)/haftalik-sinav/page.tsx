import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function WeeklyExamPage() {
  return (
    <RoutePlaceholder
      eyebrow="Deneme"
      title="Haftalık Sınav"
      description="Periyodik ölçüm ve ödül kurgusuyla ritmi görünür kılan deneme alanı."
      badge="Weekly race"
    />
  );
}
